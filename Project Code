CREATE DATABASE chicago_traffic_citations
CREATE TABLE fines (
    fine_level TINYINT NOT NULL,               -- 1 or 2
    fine_amount DECIMAL(10,2) NOT NULL,       -- 0-600
    PRIMARY KEY (fine_level, fine_amount)
);
